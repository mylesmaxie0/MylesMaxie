Ansible Playbooks
This directory includes Ansible playbooks for automating configuration on Cisco routers and switches.

Each playbook is:
Structured for modular reuse

Common use cases:

OSPF and interface provisioning
VLAN configuration
